# Заметки

Попробовал отправить разные запросы на фейковой сервис с API
GET к не\существующему ресурсу - ожидаемый результат
POST может возвращать те же данные, которые получилл. Для подтверждения + может отправить что-то допом.
DELETE вернул 200, хотя ожидалось 204. В рамках фейкового сервиса - ок, но вообще багулина

POST с пустым телом тоже принялось, что может натолкнуть на мысль, о том, что валидация на сервере не производится. Но опять же, т.к. фейковы сервис

